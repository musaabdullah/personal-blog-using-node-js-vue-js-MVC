<div class="card px-2 py-2 my-2">
    <div class="card-body">
        <h2>{{ blog.title }}</h2>
        <p>{{ blog.content.slice(0,200) }}</p>
        <a href="#" class="btn btn-primary">Read more</a>
    </div>
</div>